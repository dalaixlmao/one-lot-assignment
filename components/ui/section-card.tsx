import { WrapperFunctionTypes } from "@/lib/types";

export function WhiteSection({ children }: WrapperFunctionTypes) {
  return <div>{children}</div>;
}

export function GraySection({ children }: WrapperFunctionTypes) {
  return <div>{children}</div>;
}
